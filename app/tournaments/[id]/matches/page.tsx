"use client";

import Link from "next/link";import {useParams} from "next/navigation";import {useEffect,useState} from "react";import {supabase} from "../../../../lib/supabase";import s from "../../tournament.module.css";
type M={id:string;match_number:number;scheduled_at:string|null;court_number:number|null;score_a:number|null;score_b:number|null;status:string;duo_a_id:string|null;duo_b_id:string|null;duo_a:{name:string}|null;duo_b:{name:string}|null};
export default function Matches(){const {id}=useParams<{id:string}>();const [matches,setMatches]=useState<M[]>([]);const [error,setError]=useState("");const [busy,setBusy]=useState<string|null>(null);async function load(){const {data,error}=await supabase.from("tournament_matches").select("id,match_number,scheduled_at,court_number,score_a,score_b,status,duo_a_id,duo_b_id,duo_a:tournament_duos!tournament_matches_duo_a_id_fkey(name),duo_b:tournament_duos!tournament_matches_duo_b_id_fkey(name)").eq("tournament_id",id).order("match_number");if(error)setError(error.message);setMatches((data||[]) as any)}useEffect(()=>{load()},[id]);async function save(m:M,a:number,b:number,status="COMPLETED"){setBusy(m.id);const winnerId=status==="WALKOVER"?m.duo_a_id:a>b?m.duo_a_id:a<b?m.duo_b_id:null;const {error}=await supabase.from("tournament_matches").update({score_a:a,score_b:b,status,winner_duo_id:winnerId}).eq("id",m.id);if(error)setError(error.message);else load();setBusy(null)}return <main className={s.page}><div className={s.shell}><div className={s.brand}>LIVE MATCHES</div><h1 className={s.title}>Run the tournament</h1><p className={s.sub}>Record results, walkovers and live match status.</p>{error&&<div className={s.error}>{error}</div>}<div className={s.list}>{matches.map(m=><MatchCard key={m.id} m={m} busy={busy===m.id} onSave={save}/>)}</div>{!matches.length&&<div className={s.card}>No matches yet. Generate the draw first.</div>}<div style={{marginTop:22}}><Link href={`/tournaments/${id}`} className={s.button+" "+s.secondary}>← Dashboard</Link></div></div></main>}
function MatchCard({m,busy,onSave}:{m:M;busy:boolean;onSave:(m:M,a:number,b:number,status?:string)=>void}){const [a,setA]=useState(String(m.score_a??0));const [b,setB]=useState(String(m.score_b??0));return <div className={s.card}><div className={s.row}><div><Link href={`/tournaments/${m.id}`} className={s.pill}>Match {m.match_number}</Link>{m.court_number&&<span className={s.pill} style={{marginLeft:6}}>Court {m.court_number}</span>}</div><span className={s.muted}>{m.status}</span></div><div className={s.grid2} style={{marginTop:15}}><div><h3>{m.duo_a?.name||"BYE / TBD"}</h3><input className={s.input} type="number" min="0" value={a} onChange={e=>setA(e.target.value)}/></div><div><h3>{m.duo_b?.name||"BYE / TBD"}</h3><input className={s.input} type="number" min="0" value={b} onChange={e=>setB(e.target.value)}/></div></div><div className={s.actions} style={{marginTop:14}}><button className={s.button} disabled={busy||!m.duo_a||!m.duo_b} onClick={()=>onSave(m,Number(a),Number(b),"COMPLETED")}>{busy?"Saving…":"Save result"}</button><button className={s.button+" "+s.secondary} disabled={busy||!m.duo_a} onClick={()=>onSave(m,0,0,"WALKOVER")}>Walkover to {m.duo_a?.name||"A"}</button></div></div>}
