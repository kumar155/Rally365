"use client";

import Link from "next/link";import {useParams} from "next/navigation";import {useEffect,useState} from "react";import {supabase} from "../../../../lib/supabase";import s from "../../tournament.module.css";
type M={id:string;match_number:number;scheduled_at:string|null;court_number:number|null;status:string;duo_a?:{name:string}|null;duo_b?:{name:string}|null};
export default function Schedule(){const {id}=useParams<{id:string}>();const [date,setDate]=useState("");const [start,setStart]=useState("09:00");const [courts,setCourts]=useState("2");const [duration,setDuration]=useState("20");const [rest,setRest]=useState("5");const [matches,setMatches]=useState<M[]>([]);const [error,setError]=useState("");const [busy,setBusy]=useState(false);
 async function load(){const {data,error}=await supabase.from("tournament_matches").select("id,match_number,scheduled_at,court_number,status,duo_a:tournament_duos!tournament_matches_duo_a_id_fkey(name),duo_b:tournament_duos!tournament_matches_duo_b_id_fkey(name)").eq("tournament_id",id).order("match_number");if(error)setError(error.message);setMatches((data||[]) as any)}useEffect(()=>{load()},[id]);
 async function generate(){if(!date)return setError("Choose a schedule date.");setBusy(true);setError("");const list=[...matches];const base=new Date(`${date}T${start}:00`);const c=Math.max(1,Number(courts));const dur=Math.max(1,Number(duration));const gap=Math.max(0,Number(rest));for(let i=0;i<list.length;i++){const slot=Math.floor(i/c);const court=(i%c)+1;const when=new Date(base.getTime()+slot*(dur+gap)*60000);const {error}=await supabase.from("tournament_matches").update({scheduled_at:when.toISOString(),court_number:court}).eq("id",list[i].id);if(error){setError(error.message);break}}await supabase.from("tournament_schedules").delete().eq("tournament_id",id);await supabase.from("tournament_schedules").insert({tournament_id:id,schedule_date:date,start_at:base.toISOString(),court_count:c,match_duration_minutes:dur,rest_minutes:gap,status:"PUBLISHED"});await load();setBusy(false)}
 return <main className={s.page}><div className={s.shell}><div className={s.brand}>SCHEDULE</div><h1 className={s.title}>Courts & match times</h1><p className={s.sub}>Generate a practical running order from the current draw.</p><div className={s.card}><div className={s.grid}><div className={s.field}><label>Date</label><input className={s.input} type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><div className={s.field}><label>Start time</label><input className={s.input} type="time" value={start} onChange={e=>setStart(e.target.value)}/></div><div className={s.field}><label>Courts</label><input className={s.input} type="number" min="1" value={courts} onChange={e=>setCourts(e.target.value)}/></div><div className={s.field}><label>Match duration (min)</label><input className={s.input} type="number" min="1" value={duration} onChange={e=>setDuration(e.target.value)}/></div><div className={s.field}><label>Rest between slots (min)</label><input className={s.input} type="number" min="0" value={rest} onChange={e=>setRest(e.target.value)}/></div></div><button className={s.button} disabled={busy||!matches.length} onClick={generate}>{busy?"Scheduling…":"Generate schedule"}</button>{error&&<div className={s.error}>{error}</div>}</div><div className={s.card}><h2>Match order</h2><div className={s.list}>{matches.map(m=><div className={s.item} key={m.id}><div><strong>Match {m.match_number}</strong><div>{m.duo_a?.name||"TBD"} <span className={s.muted}>vs</span> {m.duo_b?.name||"TBD"}</div></div><div style={{textAlign:"right"}}><span className={s.pill}>Court {m.court_number||"—"}</span><div className={s.muted}>{m.scheduled_at?new Date(m.scheduled_at).toLocaleTimeString("en-IN",{hour:"numeric",minute:"2-digit"}):"Not scheduled"}</div></div></div>)}</div></div><div className={s.footerActions}><Link href={`/tournaments/${id}/draw`} className={s.button+" "+s.secondary}>Back</Link><Link href={`/tournaments/${id}`} className={s.button}>Tournament dashboard →</Link></div></div></main>}
