"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {supabase} from "../../../lib/supabase";import s from "../tournament.module.css";
type M={id:string;match_number:number;scheduled_at:string|null;court:number|null;team_a_score:number|null;team_b_score:number|null;status:string;team_a_duo_id:string|null;team_b_duo_id:string|null;team_a:{name:string}|null;team_b:{name:string}|null};
export default function Matches(){const[id,setId]=useState("");const[matches,setMatches]=useState<M[]>([]);const[error,setError]=useState("");const[busy,setBusy]=useState<string|null>(null);useEffect(()=>setId(new URLSearchParams(window.location.search).get("id")||""),[]);async function load(){if(!id)return;const{data,error}=await supabase.from("tournament_matches").select("id,match_number,scheduled_at,court,team_a_score,team_b_score,status,team_a_duo_id,team_b_duo_id,team_a:tournament_duos!tournament_matches_team_a_duo_id_fkey(name),team_b:tournament_duos!tournament_matches_team_b_duo_id_fkey(name)").eq("tournament_id",id).order("match_number");if(error)setError(error.message);setMatches((data||[])as any)}useEffect(()=>{load()},[id]);async function save(m:M,a:number,b:number,status="COMPLETED"){setBusy(m.id);const winnerId=status==="COMPLETED"?(a>b?m.team_a_duo_id:a<b?m.team_b_duo_id:null):null;const{error}=await supabase.from("tournament_matches").update({team_a_score:a,team_b_score:b,status,winner_duo_id:winnerId}).eq("id",m.id);if(error)setError(error.message);else await load();setBusy(null)}const path=(p:string)=>`/tournaments/${p}?id=${encodeURIComponent(id)}`;if(!id)return <main className={s.page}><div className={s.shell}><div className={s.card}>Tournament ID is missing.</div></div></main>;return <main className={s.page}><div className={s.shell}><div className={s.top}><div><div className={s.brand}>RALLY365 OPEN</div><h1 className={s.title}>Matches</h1><p className={s.sub}>Live scoring and completed results.</p></div><Link href={path("schedule")} className={s.secondaryButton}>Schedule →</Link></div><nav className={s.tabs}><Link className={s.tab} href={path("manage")}>Overview</Link><Link className={s.tab} href={path("schedule")}>Schedule</Link><Link className={`${s.tab} ${s.tabActive}`} href={path("matches")}>Matches</Link><Link className={s.tab} href={path("standings")}>Standings</Link></nav>{error&&<div className={s.error}>{error}</div>}<div className={s.list}>{matches.map(m=><MatchCard key={m.id} m={m} busy={busy===m.id} onSave={save}/>)}</div>{!matches.length&&<div className={s.card}>No matches yet. Generate the draw first.</div>}</div></main>}
function MatchCard({m,busy,onSave}:{m:M;busy:boolean;onSave:(m:M,a:number,b:number,status?:string)=>void}){const[a,setA]=useState(String(m.team_a_score??0));const[b,setB]=useState(String(m.team_b_score??0));const live=m.status==="LIVE";return <div className={s.matchTile}><div className={s.matchTileTop}><span className={live?s.gold:""}>{live?"● LIVE":m.status}</span><span>Match {m.match_number} · {m.court?`Court ${m.court}`:"Court TBD"}</span></div><div className={s.teamLine}><span className={s.teamIdentity}><span className={s.avatar}>A</span><span className={s.teamName}>{m.team_a?.name||"TBD"}</span></span><input className={s.scoreInput} type="number" min="0" value={a} onChange={e=>setA(e.target.value)}/></div><div className={s.teamLine}><span className={s.teamIdentity}><span className={s.avatar}>B</span><span className={s.teamName}>{m.team_b?.name||"TBD"}</span></span><input className={s.scoreInput} type="number" min="0" value={b} onChange={e=>setB(e.target.value)}/></div><div className={s.actions} style={{marginTop:10}}><Link href={`/tournaments/match?id=${m.id}`} className={s.button+" "+s.ghost}>Open</Link><button className={s.button} disabled={busy||!m.team_a_duo_id||!m.team_b_duo_id} onClick={()=>onSave(m,Number(a),Number(b),"COMPLETED")}>{busy?"Saving…":"Save score"}</button></div></div>}
