"use client";

import Link from "next/link";import {useEffect,useState} from "react";import {supabase} from "../../../lib/supabase";import s from "../tournament.module.css";
type Player={id:string;display_name:string;avatar_url:string|null;seed:number|null;status:string};
export default function Players(){const[id,setId]=useState("");const[players,setPlayers]=useState<Player[]>([]);const[name,setName]=useState("");const[search,setSearch]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>setId(new URLSearchParams(window.location.search).get("id")||""),[]);async function load(){if(!id)return;const{data,error}=await supabase.from("tournament_players").select("id,display_name,avatar_url,seed,status").eq("tournament_id",id).order("created_at");if(error)setError(error.message);setPlayers((data as Player[])||[])}useEffect(()=>{load()},[id]);async function add(e:React.FormEvent){e.preventDefault();if(!id||!name.trim())return;setBusy(true);setError("");const{error}=await supabase.from("tournament_players").insert({tournament_id:id,display_name:name.trim()});if(error)setError(error.message);else{setName("");await load()}setBusy(false)}async function remove(pid:string){const{error}=await supabase.from("tournament_players").delete().eq("id",pid).eq("tournament_id",id);if(error)setError(error.message);else await load()}const filtered=players.filter(p=>p.display_name.toLowerCase().includes(search.toLowerCase()));const path=(p:string)=>`/tournaments/${p}?id=${encodeURIComponent(id)}`;if(!id)return <main className={s.page}><div className={s.shell}><div className={s.card}>Tournament ID is missing.</div></div></main>;return <main className={s.page}><div className={s.shell}><div className={s.top}><div><div className={s.brand}>RALLY365 OPEN</div><h1 className={s.title}>Players</h1><p className={s.sub}>Tournament participants · {players.length} players</p></div><Link href={path("manage")} className={s.secondaryButton}>← Overview</Link></div><nav className={s.tabs}><Link className={s.tab} href={path("manage")}>Overview</Link><Link className={s.tab} href={path("draw")}>Draw</Link><Link className={s.tab} href={path("matches")}>Matches</Link><Link className={s.tab} href={path("standings")}>Standings</Link><Link className={`${s.tab} ${s.tabActive}`} href={path("players")}>Players</Link></nav>{error&&<div className={s.error}>{error}</div>}<div className={s.grid2}><form className={s.card} onSubmit={add}><div className={s.eyebrow}>ADD PLAYER</div><h2>Individual player</h2><p className={s.sub}>Players belong only to this tournament.</p><div className={s.field} style={{marginTop:15}}><label>Name</label><input className={s.input} value={name} onChange={e=>setName(e.target.value)} placeholder="Player name" /></div><button className={s.button} disabled={busy||!name.trim()}>{busy?"Adding…":"Add player"}</button></form><section className={s.card}><div className={s.row}><div><div className={s.eyebrow}>PLAYER ROSTER</div><h2>{players.length} players</h2></div><Link href={path("partners")} className={s.secondaryButton}>Partners →</Link></div><input className={s.input} style={{margin:"12px 0"}} value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search players…" /><div className={s.playerGrid}>{filtered.map(p=><div className={s.playerCard} key={p.id}><div className={s.avatar}>{p.avatar_url?<img src={p.avatar_url} alt=""/>:p.display_name.charAt(0).toUpperCase()}</div><strong>{p.display_name}</strong><span>{p.seed?`Seed ${p.seed}`:"Tournament player"}</span><button type="button" className={`${s.button} ${s.danger}`} style={{marginTop:9,width:"100%",padding:"7px"}} onClick={()=>remove(p.id)}>Remove</button></div>)}</div>{!filtered.length&&<p className={s.sub}>No players found.</p>}</section></div></div></main>}
